# Deployment de Grafana con datasource y dashboard aprovisionados automaticamente.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      labels:
        app: grafana
    spec:
      containers:
      - name: grafana
        image: grafana/grafana:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        env:
        # Contrasena inicial del usuario admin para la practica.
        - name: GF_SECURITY_ADMIN_PASSWORD
          value: "admin"
        volumeMounts:
        # Cada ConfigMap se monta en la ruta que Grafana inspecciona al arrancar.
        - name: grafana-datasources
          mountPath: /etc/grafana/provisioning/datasources
        - name: grafana-dashboards-provisioning
          mountPath: /etc/grafana/provisioning/dashboards
        - name: grafana-dashboard-flight
          mountPath: /var/lib/grafana/dashboards
      volumes:
      # Configuracion del datasource Prometheus.
      - name: grafana-datasources
        configMap:
          name: grafana-datasources
      # Configuracion del proveedor que carga dashboards desde ficheros.
      - name: grafana-dashboards-provisioning
        configMap:
          name: grafana-dashboards-provisioning
      # Dashboard JSON con las metricas concretas del pipeline.
      - name: grafana-dashboard-flight
        configMap:
          name: grafana-dashboard-flight
---
# Service NodePort que publica la interfaz web de Grafana.
apiVersion: v1
kind: Service
metadata:
  name: grafana
spec:
  type: NodePort
  selector:
    app: grafana
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30300
---
# ConfigMap que registra Prometheus como fuente de datos predeterminada.
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
    - name: Prometheus
      type: prometheus
      # URL interna basada en el DNS del Service prometheus.
      url: http://prometheus:9090
      # Grafana actua como proxy y el navegador no contacta directamente con Prometheus.
      access: proxy
      isDefault: true

---
# Indica a Grafana que cargue dashboards desde /var/lib/grafana/dashboards.
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-provisioning
data:
  dashboards.yaml: |
    apiVersion: 1
    providers:
    - name: default
      folder: ''
      type: file
      options:
        path: /var/lib/grafana/dashboards
---
# Dashboard serializado como JSON dentro de una clave del ConfigMap.
# Incluye totales, tasas, categorias y latencia p95; el JSON no admite comentarios internos.
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-flight
data:
  flight_pipeline.json: |
    {
      "annotations": {
        "list": [
          {
            "builtIn": 1,
            "datasource": {
              "type": "grafana",
              "uid": "-- Grafana --"
            },
            "enable": true,
            "hide": true,
            "iconColor": "rgba(0, 211, 255, 1)",
            "name": "Annotations & Alerts",
            "type": "dashboard"
          }
        ]
      },
      "editable": true,
      "fiscalYearStartMonth": 0,
      "graphTooltip": 0,
      "id": null,
      "links": [],
      "panels": [
        {
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 0,
            "y": 0
          },
          "id": 1,
          "targets": [
            {
              "expr": "flight_requests_total",
              "legendFormat": "requests",
              "refId": "A"
            }
          ],
          "title": "Solicitudes enviadas a Kafka (total)",
          "type": "stat"
        },
        {
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 6,
            "y": 0
          },
          "id": 2,
          "targets": [
            {
              "expr": "sum(flight_predictions_total)",
              "legendFormat": "predicciones",
              "refId": "A"
            }
          ],
          "title": "Predicciones recibidas (total)",
          "type": "stat"
        },
        {
          "gridPos": {
            "h": 8,
            "w": 12,
            "x": 12,
            "y": 0
          },
          "id": 4,
          "targets": [
            {
              "expr": "flight_predictions_total",
              "legendFormat": "{{category}}",
              "refId": "A"
            }
          ],
          "title": "Predicciones por categor\u00eda",
          "type": "piechart"
        },
        {
          "gridPos": {
            "h": 8,
            "w": 12,
            "x": 0,
            "y": 4
          },
          "id": 3,
          "targets": [
            {
              "expr": "rate(flight_requests_total[1m])",
              "legendFormat": "req/s",
              "refId": "A"
            }
          ],
          "title": "Tasa de solicitudes (req/s)",
          "type": "timeseries"
        },
        {
          "gridPos": {
            "h": 8,
            "w": 12,
            "x": 12,
            "y": 8
          },
          "id": 6,
          "targets": [
            {
              "expr": "rate(flask_http_request_total[1m])",
              "legendFormat": "{{method}} {{path}}",
              "refId": "A"
            }
          ],
          "title": "HTTP requests por ruta",
          "type": "timeseries"
        },
        {
          "gridPos": {
            "h": 8,
            "w": 12,
            "x": 0,
            "y": 12
          },
          "id": 5,
          "targets": [
            {
              "expr": "histogram_quantile(0.95, rate(flask_http_request_duration_seconds_bucket[5m]))",
              "legendFormat": "p95",
              "refId": "A"
            }
          ],
          "title": "Latencia HTTP Flask (p95)",
          "type": "timeseries"
        }
      ],
      "refresh": "10s",
      "schemaVersion": 42,
      "tags": [],
      "templating": {
        "list": []
      },
      "time": {
        "from": "now-6h",
        "to": "now"
      },
      "timepicker": {},
      "timezone": "",
      "title": "Flight Delay Pipeline",
      "uid": "adtdmz5",
      "version": 1,
      "weekStart": ""
    }
